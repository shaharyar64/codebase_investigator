# AI Codebase Investigator Frontend

Next.js App Router frontend for submitting repositories, asking codebase questions, and viewing grounded citations plus audit results.

## Setup

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` when the backend is not running at `http://localhost:8000/api/v1`.

